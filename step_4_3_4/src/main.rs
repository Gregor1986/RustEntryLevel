// Bool
// Напишите программу, которая создает две переменные, инициализирует их значениями true и false и выводит их 
// значения следующим образом:
// Вам нравится программировать ? true
// Вы ленитесь делать задания ? false
fn main() {
    let a = true;
        let b = false;
        println!("Вам нравится программировать ? {}", a);
        println!("Вы ленитесь делать задания ? {}", b);
    }